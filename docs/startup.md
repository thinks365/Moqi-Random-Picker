# 启动设置

## 悬浮窗启动

- **启动时自动显示悬浮窗**：打开主界面时自动显示悬浮窗。

## 开机自启

- **开机自动启动程序**：系统启动时自动运行程序（主界面模式）。
- **开机启动（仅悬浮窗）**：系统启动时仅显示悬浮窗，不打开主界面。

> ⚠️ 注意：启用「仅悬浮窗」模式后，当前窗口会立即关闭。如需打开主界面，请单击系统托盘图标。

## 自动检查

- **启动时自动检查更新**：程序启动时自动检查是否有新版本。
- **启动时自动检查名单**：启动时若无名单数据，自动弹出导入提示。
